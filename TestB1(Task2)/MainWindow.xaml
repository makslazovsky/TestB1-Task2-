<Window x:Class="TestB1_Task2_.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TestB1" Height="450" Width="950">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="excelGrid" Grid.Row="0" AutoGenerateColumns="True">
            <!--<DataGrid.Columns>
                <DataGridTextColumn Header="Б/сч" />
                <DataGridTextColumn Header="Входящее сальдо актив"  />
                <DataGridTextColumn Header="Входящее сальдо пассив" />
                <DataGridTextColumn Header="Обороты дебет"  />
                <DataGridTextColumn Header="Обороты кредит"  />
                <DataGridTextColumn Header="Исходящее сальдо актив"  />
                <DataGridTextColumn Header="Исходящее сальдо актив"  />
            </DataGrid.Columns>-->
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="Upload File" Click="UploadButton_Click" Margin="10" />
            <Button Content="Refresh List" Click="RefreshButton_Click" Margin="10" />
            <ComboBox Name="fileComboBox" Width="200" Margin="10" SelectionChanged="ComboBox_SelectionChanged"></ComboBox>
        </StackPanel>
    </Grid>
</Window>